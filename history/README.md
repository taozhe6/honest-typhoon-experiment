# Imported repository history

`ibtracs-agency-disagreement-pre-monorepo.bundle` is a complete Git bundle of the
standalone IBTrACS repository immediately before its files were imported into the
`typhoon` monorepo.

Verified source head: `4d755e621cc53e73c405f458295c6d842273ad60` (`main`).

Restore or inspect it with:

```bash
git clone ibtracs-agency-disagreement-pre-monorepo.bundle restored-ibtracs
```

